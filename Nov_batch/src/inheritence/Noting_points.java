package inheritence;

public class Noting_points {
	/* inheritance means acquiring all the properties(variables) and 
	 * behavior(methods) from one class to another class
	 * 
	 * OBJECTIVES
	 * 1.code re use ability
	 * 2. we can avoid duplication
	 * 
	 * TYPES OF INHERITANCE
	 * 1.single
	 * 2.multiple
	 * 3.hierarchical*/

}
